const Checkout = () => (
  <div className="min-h-screen flex items-center justify-center">
    <p className="text-gray-500">Page de paiement — à implémenter (Phase 2)</p>
  </div>
)
export default Checkout
