const DashboardUser = () => (
  <div className="min-h-screen flex items-center justify-center">
    <p className="text-gray-500">Espace client — à implémenter (Phase 2)</p>
  </div>
)
export default DashboardUser
